//cau hinh
export const routes = {
    login: '/login',
    register: '/register',
    logout: '/logout',
    home: '/',
};

export const apiRoutes = {
    login: '/auth/login',
    register: '/auth/register',
    logout: '/auth/logout',
};
export default routes;
