import { SideBar } from 'components/Common';
import './HomePage.scss';

function HomePage() {
    return (
        <div className="root">
            <SideBar />
        </div>
    );
}

export default HomePage;
